package Day1;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;

public class May9_Windowhandles {

	
	public static void main(String[] args) {
		/*System.setProperty("webdriver.chrome.driver", "C:\\Manipal\\chromedriver_win32\\chromedriver.exe");
		WebDriver driver = new ChromeDriver();
		driver.navigate().to("https://www.toolsqa.com/automation-practice-switch-window/");
		driver.manage().window().maximize();
		// To identify the parent window
		String Parentwindow = driver.getWindowHandle();
		System.out.println("Parent window is  " + Parentwindow);*/
		
		//WebElement Find = driver.findElement(By.id("button1"));
		// To click the child window
		

	}

}
